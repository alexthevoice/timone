/**
 * Il nome dell'applicazione, in un posto solo.
 *
 * Compare nella barra in alto, sulla schermata di accesso, nel titolo della
 * finestra e nelle pagine di errore. Per cambiarlo bastano trenta secondi:
 * o imposti NEXT_PUBLIC_APP_NAME fra le variabili d'ambiente, o cambi la
 * parola qui sotto. Non c'è nessun altro posto da cercare.
 */
export const NOME = process.env.NEXT_PUBLIC_APP_NAME || "Timone";
